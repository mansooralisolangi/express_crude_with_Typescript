# Express CRUD API

A simple **CRUD (Create, Read, Update, Delete)** API built with **Express.js**, **TypeScript**, and **MongoDB** using **Mongoose**. This project demonstrates a clean folder structure and best practices for building scalable Node.js applications.

---

## 📂 Project Structure
express_crud_with_typescript/
├── src/
│   ├── controllers/    # Request handlers
│   ├── db/            # Database connection and configuration
│   ├── middleware/    # Custom middleware functions
│   ├── models/        # Mongoose schemas and models
│   ├── routes/        # API route definitions
│   └── index.ts       # Application entry point
├── node_modules/      # Dependencies
├── .env              # Environment variables
├── .gitignore        # Git ignore rules
├── package.json      # Project dependencies and scripts
├── package-lock.json # Dependency lock file
└── tsconfig.json     # TypeScript configuration

---

## ⚡ Features

- RESTful API endpoints for CRUD operations
- MongoDB integration using Mongoose
- TypeScript support for type safety
- Structured folder organization
- Environment variable configuration with `.env`

---

## 🚀 Installation

1. **Clone the repository:**
git clone https://github.com/mansooralisolangi/express_crud_with_typescript.git
cd express_crud_with_typescript
